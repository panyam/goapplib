// Code generated by protoc-gen-dal-gorm. DO NOT EDIT.
package dal

import (
	"context"
	"errors"

	v1 "github.com/panyam/goapplib/content/gen/gorm/tags/v1"
	"gorm.io/gorm"
)

// TagGORMDAL provides database access helper methods for v1.TagGORM.
type TagGORMDAL struct {
	// TableName overrides the table for all operations.
	// If empty, uses the struct's TableName() method (if any) or GORM's default.
	TableName string

	// WillCreate hook is called when Save detects the record doesn't exist and will create it.
	// Return an error to prevent creation.
	WillCreate func(context.Context, *v1.TagGORM) error
}

// NewTagGORMDAL creates a new TagGORMDAL instance.
// If tableName is empty, operations will use the struct's TableName() method
// or GORM's default table naming convention.
func NewTagGORMDAL(tableName string) *TagGORMDAL {
	return &TagGORMDAL{TableName: tableName}
}

// db returns a *gorm.DB scoped to the correct table.
// If TableName is set, uses db.Table(); otherwise returns db unchanged
// to let GORM resolve the table name from the struct's TableName() method.
func (d *TagGORMDAL) db(db *gorm.DB) *gorm.DB {
	if d.TableName != "" {
		return db.Table(d.TableName)
	}
	return db
}

// Create creates a new v1.TagGORM record.
// Returns an error if the record already exists.
func (d *TagGORMDAL) Create(ctx context.Context, db *gorm.DB, obj *v1.TagGORM) error {
	return d.db(db).Create(obj).Error
}

// Update updates an existing v1.TagGORM record.
// Returns ErrRecordNotFound if the record doesn't exist.
// For conditional updates (optimistic locking), pass a db with WHERE conditions:
//
//	dal.Update(ctx, db.Where("version = ?", oldVersion), obj)
func (d *TagGORMDAL) Update(ctx context.Context, db *gorm.DB, obj *v1.TagGORM) error {
	result := d.db(db).Updates(obj)
	if result.Error != nil {
		return result.Error
	}

	// Check if record was found and updated
	if result.RowsAffected == 0 {
		return gorm.ErrRecordNotFound
	}

	return nil
}

// Save creates or updates a v1.TagGORM record (upsert).
// If the record doesn't exist, it will call WillCreate hook before saving.
// For conditional updates (optimistic locking), pass a db with WHERE conditions:
//
//	dal.Save(ctx, db.Where("version = ?", oldVersion), obj)
func (d *TagGORMDAL) Save(ctx context.Context, db *gorm.DB, obj *v1.TagGORM) error {
	// Validate primary key(s)
	if obj.Id == "" {
		return errors.New("primary key 'Id' cannot be empty")
	}

	// Check if record exists by trying to fetch it
	var existing v1.TagGORM
	err := d.db(db).First(&existing, "id = ?", obj.Id).Error

	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			// Record doesn't exist - call WillCreate hook before saving
			if d.WillCreate != nil {
				if err := d.WillCreate(ctx, obj); err != nil {
					return err
				}
			}
		} else {
			// Other error
			return err
		}
	}

	// Save (create or update)
	return d.db(db).Save(obj).Error
}

// Get retrieves a v1.TagGORM record by primary key.
// Returns (nil, nil) if the record is not found (not an error).
func (d *TagGORMDAL) Get(ctx context.Context, db *gorm.DB, id string) (*v1.TagGORM, error) {
	var out v1.TagGORM
	err := d.db(db).First(&out, "id = ?", id).Error
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, nil
		}
		return nil, err
	}
	return &out, nil
}

// Delete removes a v1.TagGORM record by primary key.
func (d *TagGORMDAL) Delete(ctx context.Context, db *gorm.DB, id string) error {
	return d.db(db).Where("id = ?", id).Delete(&v1.TagGORM{}).Error
}

// List retrieves multiple v1.TagGORM records using the provided query.
// The caller is responsible for adding filters, ordering, and pagination to the query.
func (d *TagGORMDAL) List(ctx context.Context, query *gorm.DB) ([]*v1.TagGORM, error) {
	var out []*v1.TagGORM
	err := d.db(query).Find(&out).Error
	return out, err
}

// BatchGet retrieves multiple v1.TagGORM records by primary key.
// Results are returned in the order provided by the database (not necessarily the input order).
func (d *TagGORMDAL) BatchGet(ctx context.Context, db *gorm.DB, ids []string) ([]*v1.TagGORM, error) {
	if len(ids) == 0 {
		return []*v1.TagGORM{}, nil
	}

	var out []*v1.TagGORM
	err := d.db(db).Where("id IN ?", ids).Find(&out).Error
	return out, err
}

// EntityTagKey represents the composite primary key for v1.EntityTagGORM
type EntityTagKey struct {
	TagId    string
	EntityId string
	TaggedBy string
}

// EntityTagGORMDAL provides database access helper methods for v1.EntityTagGORM.
type EntityTagGORMDAL struct {
	// TableName overrides the table for all operations.
	// If empty, uses the struct's TableName() method (if any) or GORM's default.
	TableName string

	// WillCreate hook is called when Save detects the record doesn't exist and will create it.
	// Return an error to prevent creation.
	WillCreate func(context.Context, *v1.EntityTagGORM) error
}

// NewEntityTagGORMDAL creates a new EntityTagGORMDAL instance.
// If tableName is empty, operations will use the struct's TableName() method
// or GORM's default table naming convention.
func NewEntityTagGORMDAL(tableName string) *EntityTagGORMDAL {
	return &EntityTagGORMDAL{TableName: tableName}
}

// db returns a *gorm.DB scoped to the correct table.
// If TableName is set, uses db.Table(); otherwise returns db unchanged
// to let GORM resolve the table name from the struct's TableName() method.
func (d *EntityTagGORMDAL) db(db *gorm.DB) *gorm.DB {
	if d.TableName != "" {
		return db.Table(d.TableName)
	}
	return db
}

// Create creates a new v1.EntityTagGORM record.
// Returns an error if the record already exists.
func (d *EntityTagGORMDAL) Create(ctx context.Context, db *gorm.DB, obj *v1.EntityTagGORM) error {
	return d.db(db).Create(obj).Error
}

// Update updates an existing v1.EntityTagGORM record.
// Returns ErrRecordNotFound if the record doesn't exist.
// For conditional updates (optimistic locking), pass a db with WHERE conditions:
//
//	dal.Update(ctx, db.Where("version = ?", oldVersion), obj)
func (d *EntityTagGORMDAL) Update(ctx context.Context, db *gorm.DB, obj *v1.EntityTagGORM) error {
	result := d.db(db).Updates(obj)
	if result.Error != nil {
		return result.Error
	}

	// Check if record was found and updated
	if result.RowsAffected == 0 {
		return gorm.ErrRecordNotFound
	}

	return nil
}

// Save creates or updates a v1.EntityTagGORM record (upsert).
// If the record doesn't exist, it will call WillCreate hook before saving.
// For conditional updates (optimistic locking), pass a db with WHERE conditions:
//
//	dal.Save(ctx, db.Where("version = ?", oldVersion), obj)
func (d *EntityTagGORMDAL) Save(ctx context.Context, db *gorm.DB, obj *v1.EntityTagGORM) error {
	// Validate primary key(s)
	if obj.TagId == "" {
		return errors.New("primary key 'TagId' cannot be empty")
	}
	if obj.EntityId == "" {
		return errors.New("primary key 'EntityId' cannot be empty")
	}
	if obj.TaggedBy == "" {
		return errors.New("primary key 'TaggedBy' cannot be empty")
	}

	// Check if record exists by trying to fetch it
	var existing v1.EntityTagGORM
	err := d.db(db).First(&existing, "tag_id = ?", "entity_id = ?", "tagged_by = ?", obj.TagId, obj.EntityId, obj.TaggedBy).Error

	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			// Record doesn't exist - call WillCreate hook before saving
			if d.WillCreate != nil {
				if err := d.WillCreate(ctx, obj); err != nil {
					return err
				}
			}
		} else {
			// Other error
			return err
		}
	}

	// Save (create or update)
	return d.db(db).Save(obj).Error
}

// Get retrieves a v1.EntityTagGORM record by primary keys.
// Returns (nil, nil) if the record is not found (not an error).
func (d *EntityTagGORMDAL) Get(ctx context.Context, db *gorm.DB, tagId string, entityId string, taggedBy string) (*v1.EntityTagGORM, error) {
	var out v1.EntityTagGORM
	err := d.db(db).First(&out, "tag_id = ? AND entity_id = ? AND tagged_by = ?", tagId, entityId, taggedBy).Error
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, nil
		}
		return nil, err
	}
	return &out, nil
}

// Delete removes a v1.EntityTagGORM record by primary keys.
func (d *EntityTagGORMDAL) Delete(ctx context.Context, db *gorm.DB, tagId string, entityId string, taggedBy string) error {
	return d.db(db).Where("tag_id = ? AND entity_id = ? AND tagged_by = ?", tagId, entityId, taggedBy).Delete(&v1.EntityTagGORM{}).Error
}

// List retrieves multiple v1.EntityTagGORM records using the provided query.
// The caller is responsible for adding filters, ordering, and pagination to the query.
func (d *EntityTagGORMDAL) List(ctx context.Context, query *gorm.DB) ([]*v1.EntityTagGORM, error) {
	var out []*v1.EntityTagGORM
	err := d.db(query).Find(&out).Error
	return out, err
}

// BatchGet retrieves multiple v1.EntityTagGORM records by primary keys.
// Results are returned in the order provided by the database (not necessarily the input order).
func (d *EntityTagGORMDAL) BatchGet(ctx context.Context, db *gorm.DB, keys []EntityTagKey) ([]*v1.EntityTagGORM, error) {
	if len(keys) == 0 {
		return []*v1.EntityTagGORM{}, nil
	}

	// Build OR query for each key combination
	query := d.db(db).Where("1 = 0") // Start with false condition
	for _, key := range keys {
		query = query.Or("tag_id = ? AND entity_id = ? AND tagged_by = ?", key.TagId, key.EntityId, key.TaggedBy)
	}

	var out []*v1.EntityTagGORM
	err := query.Find(&out).Error
	return out, err
}

// TagUsageCountsGORMDAL provides database access helper methods for v1.TagUsageCountsGORM.
type TagUsageCountsGORMDAL struct {
	// TableName overrides the table for all operations.
	// If empty, uses the struct's TableName() method (if any) or GORM's default.
	TableName string

	// WillCreate hook is called when Save detects the record doesn't exist and will create it.
	// Return an error to prevent creation.
	WillCreate func(context.Context, *v1.TagUsageCountsGORM) error
}

// NewTagUsageCountsGORMDAL creates a new TagUsageCountsGORMDAL instance.
// If tableName is empty, operations will use the struct's TableName() method
// or GORM's default table naming convention.
func NewTagUsageCountsGORMDAL(tableName string) *TagUsageCountsGORMDAL {
	return &TagUsageCountsGORMDAL{TableName: tableName}
}

// db returns a *gorm.DB scoped to the correct table.
// If TableName is set, uses db.Table(); otherwise returns db unchanged
// to let GORM resolve the table name from the struct's TableName() method.
func (d *TagUsageCountsGORMDAL) db(db *gorm.DB) *gorm.DB {
	if d.TableName != "" {
		return db.Table(d.TableName)
	}
	return db
}

// Create creates a new v1.TagUsageCountsGORM record.
// Returns an error if the record already exists.
func (d *TagUsageCountsGORMDAL) Create(ctx context.Context, db *gorm.DB, obj *v1.TagUsageCountsGORM) error {
	return d.db(db).Create(obj).Error
}

// Update updates an existing v1.TagUsageCountsGORM record.
// Returns ErrRecordNotFound if the record doesn't exist.
// For conditional updates (optimistic locking), pass a db with WHERE conditions:
//
//	dal.Update(ctx, db.Where("version = ?", oldVersion), obj)
func (d *TagUsageCountsGORMDAL) Update(ctx context.Context, db *gorm.DB, obj *v1.TagUsageCountsGORM) error {
	result := d.db(db).Updates(obj)
	if result.Error != nil {
		return result.Error
	}

	// Check if record was found and updated
	if result.RowsAffected == 0 {
		return gorm.ErrRecordNotFound
	}

	return nil
}

// Save creates or updates a v1.TagUsageCountsGORM record (upsert).
// If the record doesn't exist, it will call WillCreate hook before saving.
// For conditional updates (optimistic locking), pass a db with WHERE conditions:
//
//	dal.Save(ctx, db.Where("version = ?", oldVersion), obj)
func (d *TagUsageCountsGORMDAL) Save(ctx context.Context, db *gorm.DB, obj *v1.TagUsageCountsGORM) error {
	// Validate primary key(s)
	if obj.EntityId == "" {
		return errors.New("primary key 'EntityId' cannot be empty")
	}

	// Check if record exists by trying to fetch it
	var existing v1.TagUsageCountsGORM
	err := d.db(db).First(&existing, "entity_id = ?", obj.EntityId).Error

	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			// Record doesn't exist - call WillCreate hook before saving
			if d.WillCreate != nil {
				if err := d.WillCreate(ctx, obj); err != nil {
					return err
				}
			}
		} else {
			// Other error
			return err
		}
	}

	// Save (create or update)
	return d.db(db).Save(obj).Error
}

// Get retrieves a v1.TagUsageCountsGORM record by primary key.
// Returns (nil, nil) if the record is not found (not an error).
func (d *TagUsageCountsGORMDAL) Get(ctx context.Context, db *gorm.DB, entityId string) (*v1.TagUsageCountsGORM, error) {
	var out v1.TagUsageCountsGORM
	err := d.db(db).First(&out, "entity_id = ?", entityId).Error
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, nil
		}
		return nil, err
	}
	return &out, nil
}

// Delete removes a v1.TagUsageCountsGORM record by primary key.
func (d *TagUsageCountsGORMDAL) Delete(ctx context.Context, db *gorm.DB, entityId string) error {
	return d.db(db).Where("entity_id = ?", entityId).Delete(&v1.TagUsageCountsGORM{}).Error
}

// List retrieves multiple v1.TagUsageCountsGORM records using the provided query.
// The caller is responsible for adding filters, ordering, and pagination to the query.
func (d *TagUsageCountsGORMDAL) List(ctx context.Context, query *gorm.DB) ([]*v1.TagUsageCountsGORM, error) {
	var out []*v1.TagUsageCountsGORM
	err := d.db(query).Find(&out).Error
	return out, err
}

// BatchGet retrieves multiple v1.TagUsageCountsGORM records by primary key.
// Results are returned in the order provided by the database (not necessarily the input order).
func (d *TagUsageCountsGORMDAL) BatchGet(ctx context.Context, db *gorm.DB, entityIds []string) ([]*v1.TagUsageCountsGORM, error) {
	if len(entityIds) == 0 {
		return []*v1.TagUsageCountsGORM{}, nil
	}

	var out []*v1.TagUsageCountsGORM
	err := d.db(db).Where("entity_id IN ?", entityIds).Find(&out).Error
	return out, err
}
